// Define a function `calculateDiscount` that takes a price (number) and a discount percentage (number) as arguments.
// If the discount is greater than 50%, return a message "Discount cannot be greater than 50%".
// Otherwise, return the discounted price.