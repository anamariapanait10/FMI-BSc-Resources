// Truthy and Falsy Values:
// - Create an array containing various values like "", 0, null, undefined, and "test".
// - Use an `if` statement to check if each element is "truthy" or "falsy" and log the results